---
// Placeholder del isotipo "V" de Valtia — reemplazar por el SVG oficial.
// Hereda el color del texto (currentColor): usar text-black, text-white, etc.
const { class: className = 'h-12 w-auto' } = Astro.props;
---

<svg
  viewBox="0 0 64 64"
  fill="currentColor"
  xmlns="http://www.w3.org/2000/svg"
  class={className}
  aria-hidden="true"
>
  <path d="M12 4 L24 4 L40 60 L33 60 Z" />
  <path d="M44 8 L60 8 L48 32 Z" />
</svg>
